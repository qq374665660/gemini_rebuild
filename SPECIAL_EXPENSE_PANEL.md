# 专项经费面板设计说明

页面地址 `/expense/special/`，视图 `core/views.py:special_expense_monitor_view`，装配函数
`core/views.py:_special_ledger_rows`，模板 `core/templates/core/special_expense_monitor.html`，
解析层 `core/special_ledger.py`。

---

## 一、取数口径

**主表只有 7 列，专项经费一律取课题档案登记的额度，台账只提供已执行和本年执行。**

| 列 | 取值 | 来源 |
|---|---|---|
| 课题名称 | 台账课题名，下方小字给系统课题编号、台账类别、系统状态，可点进详情 | — |
| 专项经费 | 外部课题取 `Project.external_funding`（外部专项），院自主取 `institute_funding`（院专项） | 课题档案 |
| 已执行 | 台账「总执行额度」`executed_total` | 台账 |
| 剩余 | `专项经费 − 已执行`，**为负标红** | 计算 |
| 执行率 | `已执行 ÷ 专项经费`，带进度条，>100% 时进度条封顶但数值照实显示 | 计算 |
| 未到账 | `unreceived_amount` = 台账「归属院/地下空间课题合同经费」−「已到账经费」，**仅外部课题**；院自主显示 `-` | 台账 |
| 本年执行 | 台账「本年执行额度」`year_executed` | 台账 |

分母映射写在 `core/special_ledger.py` 的 `LEDGER_SPECIAL_BUDGET_FIELD`。
风险提醒表与结题表用同一套装配结果（`special_budget` / `executed` / `remaining` / `rate`），
不会出现同一课题两张表数字不一致。

### 与台账自身口径的差异（务必知悉）

分母换成档案额度后，面板的「剩余」与台账「可支出经费」列**不再相等**，这是口径差不是算错：

- 台账「可支出经费」= `已到账经费 − 总执行额度`（外部 18/18 行吻合该式）。
- 面板「剩余」= `档案专项经费 − 总执行额度`。
- 两者之差 = `档案专项经费 − 已到账经费`，正好就是「未到账」那一列。

以 2026-09-21 导入的基线数据（外部批次 #7、院自主批次 #8）为例，在研/延期 13 条：

| | 档案口径（当前面板） | 台账口径（改造前） |
|---|---|---|
| 专项经费 | **3405.00** | 3146.30 |
| 已执行 | 1505.37 | 1505.37 |
| 整体执行率 | **44.2%** | 47.8% |
| 剩余 | **1899.63** | 1640.94 |

**院自主 36 条完全不受影响**——档案「院专项」与院台账「预算额度」本来就是同一个数（36/36 相等）。
差异全部来自外部课题：实测 18 条里 `external_funding` 有 15 条等于台账「课题合同经费」、
14 条等于「归属院合同经费」，只有 7 条等于「已到账经费」。

### 档案疑似录小的课题会让执行率虚高

分母取档案值意味着**档案录小 = 执行率虚高**，当前有两条在研外部课题命中：

| 课题 | 档案·外部专项 | 台账·归属院合同 | 台账·已到账 | 已执行 | 面板执行率 | 面板剩余 |
|---|---|---|---|---|---|---|
| 地下空间竖向建造施工工艺研究 | 380.00 | 680.00 | 532.17 | 344.86 | **90.8%** | 35.14 |
| 城市竖井机械化掘进装备及建造技术研究 | 300.00 | 500.00 | 391.30 | 235.91 | 78.6% | 64.09 |

第一条已经进了「执行风险提醒」。按台账的 680.00 算其实是 50.7%，所以**先看档案要不要改成 680，
再决定要不要催执行**。第二条是重名后人工指定归属的课题（`CSCEC-PT-016-03`），改金额前先确认挂对了课题。

反过来，档案比到账多也是常态（立项 100 只拨到 50 的 `CSCEC-2024-Z-28` 就是这种），
这类课题面板剩余会比台账可支出大，差额就是未到账部分，属正常。

### 为什么不做自动对齐

历史上出过金额被自动判定后串挂到别的课题上的问题，金额挂错的代价远大于多留一个提醒。
因此面板**不会因为台账有值就去改 `Project.external_funding` / `institute_funding`**，
也不会因为系统有值就去修台账明细；档案与台账对不上时只呈现，判断交回给人。

> 若想让外部差异只反映「真正的录入错误」而非「到账进度」，需要人工逐条核对档案后修订
> `Project.external_funding`，面板会自动跟随。**系统不会代劳这一步。**

---

## 二、整页设计

### 面板定位

**只看专项经费（外部专项 + 院专项）的执行情况，重点是在研和延期课题。**

与 `/expense/`（归集经费监控）的分工，两者取数来源完全不同，不可混用：

| | 专项经费面板 `/expense/special/` | 归集经费监控 `/expense/` |
|---|---|---|
| 看什么 | 专项经费用到哪了 | 课题总经费有没有超支 |
| 分母来源 | 课题档案 `external_funding` / `institute_funding` | 课题档案 `total_budget` |
| 分子来源 | 台账「总执行额度」 | 6606 带符号期末余额（期初+本年累计借−贷） |
| 覆盖课题 | 有专项经费的课题 | 全部课题 |
| 口径 | 全周期 | 全周期 |

### 数据流

```
《外部立项课题经费管理台账》.xlsx ┐
《院自主立项课题经费管理台账》.xlsx ┘→ parse_special_ledger() → SpecialLedgerImport（批次）
                                                        └──→ SpecialLedgerRow（每课题一行）
                                                                     │
                                          课题名归一化等值匹配 Project │ 重名→SpecialLedgerAssignment
                                                                     ↓
                    _special_ledger_rows()：专项经费←课题档案，已执行/本年执行←台账，剩余与执行率现算
                                                                     ↓
                                                          special_expense_monitor.html
```

关键实现约束：

1. **表头写「单位：万元」，实际填的是元**，解析层统一 ÷10000（`LEDGER_UNIT_DIVISOR`），
   并有量级上限断言——万元合计超过 100 万万元直接拒绝入库，防单位没换算。
2. **只按表头文本定位列，不写死列号**。两本台账列位不同（外部从第 9 列、院自主从第 10 列起），
   且明细 sheet 名有前导空格和截断名，**面板不解析明细 sheet**。
3. Excel 公式单元格带浮点尾差（`261491.019999999`），入账前按元两位小数取整，否则合计会出现
   `4354.499999999996` 这类脏数。
4. 台账行与课题**只认同名唯一匹配**，不做模糊匹配；重名一律进「系统重名待确认」等人工指定。
   人工指定写入 `SpecialLedgerAssignment`，**后续重新导入会自动复用**（`special_ledger.py`
   先查指定表，命中即 `match_state='manual'`），不用每次重名都点一遍。

### 页面分区（自上而下）

| 区块 | 内容 | 数据来源 |
|---|---|---|
| 顶部筛选 | 全部专项课题 / 外部立项课题 / 院自主立项课题，同时过滤指标卡、主表和忽略区 | `?ledger_type=` |
| 上传经费管理台账 | 两本台账各一个上传按钮 + 各批次的导入时间、源表课题数、已关联数 | 仅系统管理员可上传 |
| 6 张指标卡 | 在研/延期课题数、在研专项经费、已执行、整体执行率、剩余、本年执行 | `active_summary` |
| 执行风险提醒 | 剩余为负 / 执行率 ≥ 90% / 超到账 | `risk_rows` |
| 相比上次台账执行额增加 | 与**上一批次**同课题「总执行额度」的增量，无上一批次时整块不显示 | `growth_rows` |
| **在研 / 延期专项课题执行** | 主表，7 列 | `active_rows` |
| 系统重名待确认 | 同名多课题的台账行，提供指定归属表单 | `pending_rows` |
| 结题 / 终止等其他状态课题 | 不进头部指标的课题，含台账进度与系统状态对照 | `closed_rows` |
| 台账有、系统无（已忽略） | 系统里没建档的台账课题 | `ignored_rows` |

指标卡的「剩余」是逐行 `专项经费 − 已执行` 求和，与主表逐行相加必然一致。

### 分账规则

- **「在研」由系统 `Project.status` 决定，不采信台账「研发进度」列。** 在研/延期才计入头部指标。
  台账进度与系统状态不一致时，在「结题/终止等其他状态」区以「台账进度 | 系统状态」两列并排对照显示。
- **以系统内已有课题为准，系统没有的课题直接忽略**，只列在「台账有、系统无」区，不参与任何合计
  （当前 11 个，全部来自外部台账）。要纳入必须先在课题管理建档，再重新导入台账。
- 台账行关联到课题后，**课题档案里对应类别的专项经费为空时该行专项经费显示为空**，
  剩余与执行率一并留空，不会拿台账额度顶上去。

### 导入与批次

- 每次上传按 `SHA256 + format_version` 判重（`unique(ledger_type, file_sha256)`），**同文件重复导入不产生新批次**。
- 批次只追加不覆盖，面板读**每类台账最新一批**，上一批用于「相比上次台账执行额增加」。
- 原始文件留存在 `zichouktfeiyong/专项台账/`，按 `external_` / `institute_` 前缀分名。
- 一键重建初始数据：`manage.py seed_expense_initial`（不带 `--yes` 为干跑预览，会自动备份库到 `backups/`）。

### 当前快照下的空区块

打开页面时有几个区块是空的，属正常而不是漏数据：

- **相比上次台账执行额增加**：整块不显示。每类台账只有 1 个批次，没有上一批次可比。
- **系统重名待确认**：0 条。导入时外部、院自主各有 1 条重名，已在 2026-09-22 人工指定归属
  （`SpecialLedgerAssignment` 2 条，对应 `CSCEC-PT-016-03`、`ZJXKKT-2022-05`）。

### 权限

只读账号可看面板（`special_expense_monitor` 在 `AccessControlMiddleware.readonly_get_views` 内）；
导入台账、指定重名归属仅系统管理员。
