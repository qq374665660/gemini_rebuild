# AI分析功能配置说明

## 功能介绍

本系统集成了AI文档分析功能，支持自动分析课题申报书和任务书，提取研究内容和产出指标。
同一套模型配置也供课题智能助手使用，三个功能入口都可以选择已启用且连接测试通过的模型。

## 支持的AI服务

### 1. DeepSeek API
- **优点**: 专业的AI模型，分析质量高
- **获取方式**: 访问 https://platform.deepseek.com/ 注册获取API密钥
- **费用**: 按使用量计费

### 2. Kimi API (Moonshot)
- **优点**: 中文理解能力强，适合中文科研文档
- **获取方式**: 访问 https://platform.moonshot.cn/ 注册获取API密钥  
- **费用**: 按使用量计费

### 3. 本地模型（OpenAI 兼容接口）
- **当前地址**: `http://192.168.0.182:8000`
- **当前模型**: `XKY-AI`
- **接口**: `/v1/chat/completions`
- **鉴权**: 当前内网部署无需API密钥；如果以后启用鉴权，可在配置页填写密钥

## 推荐配置方法

使用管理员账号进入“AI API配置管理”页面，选择服务类型，填写或确认服务地址、模型名称和API密钥，然后点击“测试连接”。只有启用且测试通过的配置会出现在功能页面的模型选择器中。

本地模型已预置上述地址和模型名称，部署迁移后只需确认连接测试结果。

## 环境变量配置（兼容旧方式）

### 方法1: 直接在代码中配置

编辑 `project_manager/settings.py` 文件：

```python
# AI Analysis API Configuration
DEEPSEEK_API_KEY = 'sk-your-deepseek-api-key-here'  # 填入您的DeepSeek API密钥
KIMI_API_KEY = 'sk-your-kimi-api-key-here'          # 填入您的Kimi API密钥
```

### 方法2: 使用环境变量（推荐）

在系统环境变量中设置：

**Windows:**
```cmd
set DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
set KIMI_API_KEY=sk-your-kimi-api-key-here
```

**Linux/macOS:**
```bash
export DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
export KIMI_API_KEY=sk-your-kimi-api-key-here
```

## 文件格式支持

- **TXT**: 支持 UTF-8、GBK 等常见编码
- **DOCX**: 直接抽取正文和表格内容
- **DOC**: 通过服务器上的 Microsoft Word 或 LibreOffice 转换后分析
- **PDF**: 抽取文本并保留页码标记；扫描版PDF需要先进行OCR

## 使用说明

1. **配置模型**: 至少配置并测试通过一个云端或本地模型
2. **选择模型**: 智能助手、研究内容分析和产出指标分析均可选择使用哪套模型配置
3. **上传文档**: 在课题详情页面的"研究内容分析"或"产出指标分析"tab中上传文档
4. **等待分析**: 系统会自动调用AI服务进行分析
5. **查看结果**: 分析完成后会显示结果，可以进一步手动编辑
6. **自动同步**: 研究内容分析的结果会自动同步到课题信息的"主要研究内容"字段

## 故障排除

### 常见问题

1. **分析失败: 模型未配置或未测试通过**
   - 在“AI API配置管理”页面检查启用状态和测试结果
   - 云端服务需确保API密钥格式正确（通常以 `sk-` 开头）；当前本地模型无需密钥

2. **分析失败: 网络连接超时**
   - 检查网络连接
   - 如果在内网环境，可能需要配置代理

3. **分析失败: 文件格式不支持**
   - 确认文件为 TXT、DOC、DOCX 或 PDF
   - 扫描版 PDF 需要先进行 OCR

4. **分析失败: API调用限制**
   - 检查API账户余额
   - 确认API调用频率限制

### 备选方案

如果AI分析不可用，系统提供手动编辑功能：
1. 点击"开始编辑"按钮
2. 在文本框中输入分析结果
3. 点击"保存更改"

## 技术说明

- 用户明确选择模型时只使用所选配置；未选择时按 DeepSeek、Kimi、本地模型顺序自动回退
- 分析结果包含置信度评分和处理时间
- 所有分析结果都保存在数据库中，支持历史记录查看
- 系统会记录使用的AI服务类型，便于问题诊断

## 隐私说明

- 使用云端模型时文档内容会发送到对应服务商；使用本地模型时内容只在内网模型服务中处理
- 请确保上传的文档不包含敏感信息
- 分析结果仅存储在本地数据库中

## 更新说明

如需支持更多AI服务或文件格式，请联系系统开发团队。
